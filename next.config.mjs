/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // Opcional: Añadir una barra inclinada al final de las rutas
    trailingSlash: true,
    // Opcional: Cambiar el directorio de salida (por defecnpto es 'out')
    // distDir: 'dist',
}

export default nextConfig

